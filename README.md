# vpntui

A TUI application written in Nim that allows for easy switching between VPN servers.

Requires `wireguard` as a dependency. Put `.conf` files in a `~/.config/vpn/` directory
to be accessed by the program.

To build, run `nim -d:release c main.nim`.

Screenshot of program:

